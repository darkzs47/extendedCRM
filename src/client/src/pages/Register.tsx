import {memo, type FC} from 'react'

const Register: FC = () => {
    return (
        <main>
            {/*    login form
        button - register
        */}
        </main>
    )
}

export default memo(Register);